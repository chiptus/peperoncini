{
  "name": "david",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "concurrently": "^3.4.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "sudo service mongod start && concurrently 'cd ./client-web && yarn start' 'cd ./server && yarn run dev'",
    "start": "concurrently 'cd ./client-web && yarn run build' 'cd ./server && yarn start'",
    "postinstall": "concurrently 'cd server && yarn' 'cd client-web && yarn'"
  },
  "engines": {
    "npm": "4.1.2",
    "node": "7.5.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
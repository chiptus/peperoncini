{
  "name": "david",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "concurrently": "^3.4.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently 'cd ./client-web && yarn start' 'cd ./server && nodemon server.js'",
    "start": "concurrently 'cd ./client-web && yarn build' 'cd ./server && yarn start'",
    "postinstall": "concurrently 'cd server && yarn' 'cd client-web && yarn'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}